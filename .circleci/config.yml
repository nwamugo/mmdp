version: 2
jobs:
  deploy:
    docker:
      - image: debian:stretch

    steps:
      - run:
          name: Deploy app
          command: chmod +x ./scripts/deploy.sh && ./scripts/deploy.sh

workflows:
  version: 2

  deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only:
                - develop
                - master
                - deploy-sandbox
                - ch-circleci-deploy-job-MMDP-788
